{
  "name": "claude-codex-account-switch",
  "version": "2.2.0",
  "private": true,
  "description": "Keyboard-driven account switcher for Claude Code and Codex with isolated auth stores and rollback.",
  "author": "LightZirconite",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "engines": {
    "node": ">=20.3.0"
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "dev": "node esbuild.mjs --watch",
    "start": "node dist/cli.js",
    "dry-run": "node dist/cli.js --dry-run",
    "test": "tsx --test tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "clipboardy": "^4.0.0",
    "ink": "^5.1.0",
    "jsonc-parser": "^3.3.1",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^18.3.12",
    "esbuild": "^0.28.1",
    "tsx": "^4.23.0",
    "typescript": "^5.7.2"
  }
}
